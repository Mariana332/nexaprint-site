const v4db=window.supabase.createClient(window.NEXA_CONFIG.SUPABASE_URL,window.NEXA_CONFIG.SUPABASE_KEY);const v4esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
window.priceSearch=async function(term){const box=document.querySelector('#priceProducts');if(!box)return;term=(term||'').trim();if(!term){box.innerHTML='<div class="note">Digite o nome do produto para carregar configurações e preços.</div>';return}const {data:ps,error}=await v4db.from('products').select('id,name').ilike('name',`%${term}%`).order('name').limit(10);if(error||!ps?.length){box.innerHTML='<div class="note">Nenhum produto encontrado.</div>';return}box.innerHTML='';for(const p of ps){const {data:vs}=await v4db.from('product_variants').select('id,configuration_name,size,material,printing,finish,cut_type').eq('product_id',p.id).order('configuration_name').limit(100);const wrap=document.createElement('div');wrap.className='panel';wrap.innerHTML=`<h3>${v4esc(p.name)}</h3>`+(vs||[]).map(v=>`<div class="variant-price"><strong>${v4esc(v.configuration_name||'Configuração')}</strong><div class="hint">${v4esc([v.size,v.material,v.printing,v.finish,v.cut_type].filter(Boolean).join(' • '))}</div><div id="prices-${v.id}" class="table-wrap"><div class="muted" style="padding:10px">Carregando…</div></div></div>`).join('')||'<div class="muted">Sem configurações.</div>';box.appendChild(wrap);for(const v of vs||[])window.loadPrices(v.id)}};
