name: NEXA Import Gabaritos V7 Once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/nexa-import-gabaritos-once.yml'

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - name: Importar gabaritos V7
        run: |
          set -e
          curl --fail-with-body --silent --show-error --location \
            --request POST \
            --url 'https://pxqplmzflbtatytqyoqs.supabase.co/functions/v1/nexa-import-gabaritos-v7?token=6dqg4j6MTp6c-Gwms_v8aFSs4TLTkH1y' \
            --header 'Content-Type: application/json' \
            --data '{}'